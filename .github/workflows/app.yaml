name: app
on:
  push:
    branches:
      - main
      - ahmed-token-test
  workflow_dispatch:

jobs:
  ahmed-token-test:
    permissions:
      id-token: write
      packages: read
      contents: read
    steps:
    - name: Get Token
      run: |
        echo $ACTIONS_ID_TOKEN_REQUEST_TOKEN
